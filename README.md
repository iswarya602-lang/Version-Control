# Version-Control
Purpose of repo creation is to practice Git commands.

Complete CLI Commands 
git init

git status

git add .

git commit -m "Initial commit"

git remote add _____(github account)
git branch -M main

git push -u origin main

